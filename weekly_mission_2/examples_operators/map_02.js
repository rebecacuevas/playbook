// Uso de Map para convertir todos los nombres de una lista a minúsculas 

const names = ['Mario', 'Danna', 'Matt']
const namesToUpperCase = names.map((name) => name.toUpperCase())

console.log("Uso de Map para convertir todos los nombres de una lista en mayúsculas")
console.log(namesToUpperCase)